# LEXIS Stage 2 — BM25 search

Status: **frozen**.

## Purpose

Rank documents with BM25 for a query string.

## Required

```text
List<ScoredDoc> search(query, k)
```

- Classic BM25 parameters `k1`, `b` configurable
- Deterministic ordering for ties (docId ascending)

## Tests

Tiny corpus under `testdata/corpus/` with expected top-doc fixtures.
